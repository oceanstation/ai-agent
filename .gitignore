# compiled output
dist
tmp
out-tsc
# TypeScript 增量编译缓存（composite/incremental 开启时由 tsc 自动生成，纯本地缓存）
*.tsbuildinfo

# local env files
.env
.env.*.local

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# npm (legacy, kept for safety)
package-lock.json

# pnpm
.pnpm-store/
.pnpm-debug.log*

# System Files
.DS_Store
Thumbs.db

.nx/cache
.nx/workspace-data
.nx/migrate-runs
.claude/worktrees
.claude/settings.local.json
.codebuddy

vite.config.*.timestamp*
vitest.config.*.timestamp*
test-output

# 用户agent相关产物，避免隐私外泄
/workspace
.memory/
.data/

# HuggingFace 模型缓存（agent + chroma 共享，首次运行自动下载 ~400MB）
/.models
